/* global window */
export const cssPrefix = 'x-spreadsheet';
export const dpr = window.devicePixelRatio || 1;
export const dprf =function (){return window.devicePixelRatio || 1};
export default {
  cssPrefix,
  dpr,
  dprf
};
